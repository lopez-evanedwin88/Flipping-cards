import React, {FunctionComponent} from 'react';
import {View} from 'react-native';

import styles from './styles';

interface CustomProps {
  title: string;
}

const FlipCard: FunctionComponent<CustomProps> = ({}) => <View />;
export default FlipCard;
